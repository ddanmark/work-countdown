package com.workcountdown.app;

/**
 * 中尺寸（4×2）小部件 Provider。
 * 继承主 Provider，仅用于在桌面添加时作为独立选项出现。
 * 布局由父类 updateWidget 按 Provider 类名（含 "Medium"）选择 5×2 三圈布局。
 */
public class WorkCountdownWidgetProviderMedium extends WorkCountdownWidgetProvider {
}
