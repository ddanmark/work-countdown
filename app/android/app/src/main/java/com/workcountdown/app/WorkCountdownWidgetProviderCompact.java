package com.workcountdown.app;

/**
 * 圆环尺寸（2×2）小部件 Provider。
 * 继承主 Provider，仅用于在桌面添加时作为独立选项出现。
 * 布局选择由父类按 Provider 类名判断。
 */
public class WorkCountdownWidgetProviderCompact extends WorkCountdownWidgetProvider {
}
