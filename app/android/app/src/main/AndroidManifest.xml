<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- 桌面小部件（小尺寸 2×1） -->
        <receiver
            android:name=".WorkCountdownWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.workcountdown.app.WIDGET_TICK" />
                <action android:name="com.workcountdown.app.WIDGET_REFRESH" />
                <!-- 重启/应用更新后闹钟全部清空，需重新点燃每秒 tick 链 -->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_small_info" />
        </receiver>

        <!-- 桌面小部件（中尺寸 4×2）。tick/refresh 用显式 Intent 指向基类，
             这里的自定义 action 永远匹配不到，已移除 -->
        <receiver
            android:name=".WorkCountdownWidgetProviderMedium"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_medium_info" />
        </receiver>

        <!-- 桌面小部件（圆环尺寸 2×2） -->
        <receiver
            android:name=".WorkCountdownWidgetProviderCompact"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_compact_info" />
        </receiver>

    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 精确闹钟：每秒刷新桌面倒计时所必需。
         - SCHEDULE_EXACT_ALARM：Android 12/12L(API 31-32)安装时自动授予；
           USE_EXACT_ALARM 在这两个版本还不存在，没有这条 setExact 会抛 SecurityException，
           导致 onUpdate 在渲染完后抛异常、tick 永远续约不上（倒计时不动）。
         - USE_EXACT_ALARM：Android 13+(API 33)自动授予。两条都声明即可全版本覆盖。 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- 开机重启后恢复 widget 的每秒刷新链 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
</manifest>
